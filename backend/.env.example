# Copy this file to .env and adjust values as needed.
PORT=4000
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# Cloud database (MongoDB Atlas). Leave unset to use the local JSON files.
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster-host>/petcare?retryWrites=true&w=majority
# MONGODB_DB=petcare
